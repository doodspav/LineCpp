#ifndef TYPE_ALIASES_H
#define TYPE_ALIASES_H

#include <string>
#include <tuple>
#include <unordered_map>

using string_pair = std::tuple<std::string, std::string>;
using string_pair_umap = std::unordered_map<std::string, std::string>;

#endif // !TYPE_ALIASES_H
